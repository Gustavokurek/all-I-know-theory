// (condição) ? "valor se sim" : "valor senão" 

//exemplo

const pontuação= 500
const évip= pontuação>=1000 ? "é vip" : "não é vip"

console.log(évip)

const corUser= null;
const corPadrão= corUser|| "preto";

console.log(corPadrão)